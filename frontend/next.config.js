/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable experimental app dir (not needed in newer versions of Next.js)
  // The app directory is now stable and enabled by default
};

module.exports = nextConfig;